<?php
/*
  // Connecting to the MySQL server
  $host="localhost";
  $user_name="root";
  $pwd="Stefi0406";
  $database_name="test"; //assuming you created this
  $db=mysql_connect($host, $user_name, $pwd, $database_name);
  
  if (mysql_error() > "") print mysql_error() . "<br>";
  mysql_select_db($database_name, $db);
  if (mysql_error() > "") print mysql_error() . "<br>";
  // Storing form values into PHP variables
  // Since method="post" in the form
  $email = $_POST["email"];
  // Inserting these values into the MySQL table
  // we created above
  $query = "insert into subscribe (emails) values ('" . $email . "')";
  $result = mysql_query($query);
  // mysql_query() is a PHP function for executing
  // MySQL queries
  
  */
?>